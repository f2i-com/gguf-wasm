[package]
name = "gguf-tokenizer"
description = "Byte-level BPE over a GGUF file's own vocabulary, with the pre-tokenizer that vocabulary was trained with."
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]

[features]
default = []
std = []
